node_modules
dist
dist-ssr

# Valhalla graph data: OSM extracts, built tiles, generated config. Hundreds
# of MB, rebuilt from valhalla/README.md. Only docs/compose/config recipes
# are tracked.
valhalla/data
valhalla/extracts
*.local

# Environment files — never commit. .env.example IS allowed (no secrets).
.env
.env.*
!.env.example

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite cache
.vite

# TS incremental build info
*.tsbuildinfo

# Dev server log
dev.log
dev-lan.log
dev-lan.err.log
